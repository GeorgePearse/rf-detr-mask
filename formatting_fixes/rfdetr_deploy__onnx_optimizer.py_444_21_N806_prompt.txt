Fix the following Python formatting error N806 in rfdetr/deploy/_onnx/optimizer.py at line 444, column 21:

Error details:
    |
442 |                     self.graph.outputs[index] = layerNormV
443 |                 else:
444 |                     lastNode = betaNode.o() if betaNode.o().op == "Cast" else betaNode
    |                     ^^^^^^^^ N806
445 |                     for subNode in self.graph.nodes:
446 |                         if lastNode.outputs[0] in subNode.inputs:
    |
rfdetr/deploy/_onnx/optimizer.py:445:25: N806 Variable `subNode` in function should be lowercase
    |

Here's the current code:
                    index = self.graph.outputs.index(beta_node.outputs[0])
                    self.graph.outputs[index] = layer_norm_v
                else:
                    last_node = beta_node.o() if beta_node.o().op == "Cast" else beta_node
                    for sub_node in self.graph.nodes:
                        if last_node.outputs[0] in sub_node.inputs:
                            index = sub_node.inputs.index(last_node.outputs[0])
                            sub_node.inputs[index] = layer_norm_v
                    last_node.outputs = []

Return only the corrected code snippet, nothing else. No explanations.
