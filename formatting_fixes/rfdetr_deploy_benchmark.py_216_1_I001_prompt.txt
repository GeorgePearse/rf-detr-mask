Fix the following Python formatting error I001 in rfdetr/deploy/benchmark.py at line 216, column 1:

Error details:
    |
216 | import torchvision.transforms.functional as F
    | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ I001
217 |
218 | class ToTensor:
    |
    = help: Organize imports
rfdetr/deploy/benchmark.py:216:8: N812 Lowercase `functional` imported as non-lowercase `F`
    |
216 | import torchvision.transforms.functional as F

Here's the current code:
        format_string += "\n)"
        return format_string

class ToTensor:
    def __call__(self, img, target):
        return F.to_tensor(img), target


class Normalize:

Return only the corrected code snippet, nothing else. No explanations.
