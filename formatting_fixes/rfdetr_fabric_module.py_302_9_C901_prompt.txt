Fix the following Python formatting error C901 in rfdetr/fabric_module.py at line 302, column 9:

Error details:
    |
300 |         return metrics
301 |
302 |     def validation_step(self, batch):
    |         ^^^^^^^^^^^^^^^ C901
303 |         """Execute a single validation step.
    |
rfdetr/fabric_module.py:381:9: C901 `export_model` is too complex (12 > 10)
    |
379 |         return {"metrics": val_metrics, "results": res, "targets": targets}

Here's the current code:

    def validation_step(self, batch):
        """Execute a single validation step.

        Args:
            batch: Tuple of (samples, targets)

        Returns:
            tuple: (metrics, results, targets) for COCO evaluation

Return only the corrected code snippet, nothing else. No explanations.
