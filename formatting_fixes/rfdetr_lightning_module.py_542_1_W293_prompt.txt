Fix the following Python formatting error W293 in rfdetr/lightning_module.py at line 542, column 1:

Error details:
    |
540 | …                     if map_value > self.best_map:
541 | …                         self.best_map = map_value
542 | …                         
^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ W293
543 | …                     # Update the logged value with the computed one
544 | …                     self.log("val/mAP", map_value, on_step=False, on_epoch=True, sync_dist=True)
    |
    = help: Remove whitespace from blank line
rfdetr/lightning_module.py:554:1: W293 [*] Blank line contains whitespace

Here's the current code:
                        stats = self.coco_evaluator.coco_eval["bbox"].stats
                        if hasattr(stats, "tolist"):
                            # Log mAP
                            map_value = float(stats[0]) if stats[0] is not None else 0.0

                            # Track best model
                            if map_value > self.best_map:
                                self.best_map = map_value
                                

Return only the corrected code snippet, nothing else. No explanations.
