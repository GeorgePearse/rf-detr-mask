Fix the following Python formatting error W293 in rfdetr/lightning_module.py at line 568, column 1:

Error details:
    |
566 |         else:
567 |             print("No COCO evaluator available. Using default metrics.")
568 |             
    | ^^^^^^^^^^^^ W293
569 |         # Log any additional metrics that might be useful
570 |         if len(self.val_metrics) > 0:
    |
    = help: Remove whitespace from blank line
rfdetr/lightning_module.py:575:9: C901 `configure_optimizers` is too complex (14 > 10)

Here's the current code:
                        stats = self.coco_evaluator.coco_eval["segm"].stats
                        if hasattr(stats, "tolist"):
                            # Log mask mAP
                            mask_map_value = float(stats[0]) if stats[0] is not None else 0.0
                            if "segm" in self.postprocessors:
                                self.log("val/mask_mAP", mask_map_value, on_step=False, on_epoch=True, sync_dist=True)
                                # Also log with underscore format
                                self.log("val_mask_mAP", mask_map_value, on_step=False, on_epoch=True, sync_dist=True)
                else:

Return only the corrected code snippet, nothing else. No explanations.
