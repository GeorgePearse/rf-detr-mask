Fix the following Python formatting error W293 in rfdetr/lightning_module.py at line 503, column 1:

Error details:
    |
501 |                 # Check if we have enough data to evaluate
502 |                 eval_imgs_valid = False
503 |                 
    | ^^^^^^^^^^^^^^^^ W293
504 |                 if hasattr(self.coco_evaluator, "eval_imgs") and self.coco_evaluator.eval_imgs:
505 |                     for iou_type, imgs in self.coco_evaluator.eval_imgs.items():
    |
    = help: Remove whitespace from blank line
rfdetr/lightning_module.py:505:25: B007 Loop control variable `iou_type` not used within loop body

Here's the current code:
        self.log("val_mAP", 0.0, on_step=False, on_epoch=True, sync_dist=True)
        self.log("val_best_mAP", self.best_map, on_step=False, on_epoch=True, sync_dist=True)

        # Try to use COCO evaluator if available
        if self.coco_evaluator is not None:
            try:
                # Check if we have enough data to evaluate
                eval_imgs_valid = False
                

Return only the corrected code snippet, nothing else. No explanations.
