Fix the following Python formatting error C901 in rfdetr/lightning_module.py at line 575, column 9:

Error details:
    |
573 |             self.log("val/avg_loss", avg_loss, on_step=False, on_epoch=True, sync_dist=True)
574 |
575 |     def configure_optimizers(self):
    |         ^^^^^^^^^^^^^^^^^^^^ C901
576 |         """Configure optimizers and learning rate scheduler for iteration-based training."""
    |
rfdetr/main.py:539:9: F841 Local variable `start_time` is assigned to but never used
    |
538 |         print("Start training")

Here's the current code:
                                self.log("val_mask_mAP", mask_map_value, on_step=False, on_epoch=True, sync_dist=True)
                else:
                    print("Skipping COCO evaluation - not enough evaluation images")
            except Exception as e:
                print(f"Error during COCO evaluation: {e}. This can happen with small validation sets.")
        else:
            print("No COCO evaluator available. Using default metrics.")

        # Log any additional metrics that might be useful

Return only the corrected code snippet, nothing else. No explanations.
